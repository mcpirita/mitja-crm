# План: Восстановление CRM, переборка базы голосом, пайплайн продажи здания

**Статус:** `активный`

**Старт:** 2026-09-01
**Родительский план:** `2026-05-19-mvp-fase-1.md` (приостановлен).

---

## Контекст

CRM не использовалась с 9 июня 2026 (последнее касание прод-базы). Дмитрий:
«неудобно заполнять, накопились сотни клиентов, поэтому не пользовался».

Разбор 2026-09-01 показал, что дело **не в удобстве ввода**, а в том, что
инструмент не звал обратно: `next_action_due` пуст у всех лидов → страница
«Сегодня» пустая → возвращаться некуда.

**Ключевое решение:** голосовой ввод в CRM **не строим**. Роль голосового
интерфейса выполняет агент в чате Claude Code — Дмитрий диктует, агент пишет
напрямую в прод-базу Turso и показывает результат. Если через практику выяснится,
что не хватает мобильности (ввод с телефона) — тогда вернёмся к Telegram-боту
из `2026-05-19-mvp-fase-2-telegram-snooze.md`.

**Новый этап:** помимо аренды помещений появляется **продажа здания целиком**.
Это отдельный пайплайн с другой аудиторией (инвесторы/покупатели, не арендаторы).
Таблицу новых лидов Дмитрий выгрузит отдельно.

---

## Факты (проверено 2026-09-01, не перепроверять заново)

**Прод.**
- Turso `mitja-crm` → `libsql://mitja-crm-mcpirita.aws-eu-west-1.turso.io`
- 93 лида · 131 событие · 1 доп. контакт · 9 помещений · 24 сегмента · **0 шаблонов**
- `max(updated_at)` = `2026-06-09 09:09:23`
- Статусы: contacted 25, replied_not_interested 22, replied_interested 11,
  fup2_sent 10, fup1_sent 9, awaiting_reply 5, replied_later 5, dead 2, lost 2, new 2
- Сайт https://mitja-crm.vercel.app отвечает `401` — basic auth middleware работает,
  деплой жив.
- `turso` CLI авторизован, прямой доступ к проду есть.

**Локально.**
- `local.db` устарел: 89 лидов, `2026-05-28`. **Источник правды — прод.**
- `node_modules` с мая, нужен `npm install`. node v24.14.1, npm 11.11.0.
- Git: 121 «изменённый» файл — это **только смена прав** `100644 → 100755`
  (переезд репо на внешний SSD `/Volumes/SSD 2`), контентных диффов ноль.
  Плюс мусор AppleDouble `._*` в untracked.

**Что сломано.**
1. `next_action_due` пуст у **всех 93** лидов → «Сегодня» пустой.
2. `email_templates` пуст (0 строк) — генератор писем есть, базы шаблонов нет.
3. Даты в `outreach_events` в диапазоне `2026-03-23 … 2026-11-10` — часть в
   будущем относительно 2026-09-01. Кривой парсинг дат при импорте Lumiera
   (`scripts/import-lumiera.ts`). Ломает любую логику «кому пора написать».

---

## Правила работы (обязательные)

1. **Никаких записей в прод до бэкапа** (Фаза 0).
2. Перед любым пакетным изменением — **превью**: что меняется, у скольких строк,
   каких именно. Применять только после подтверждения Дмитрия.
3. Статусы и типы событий — строго из `CHECK constraint` в `src/lib/db/schema.sql`.
   Не выдумывать новые значения.
4. Работаем **пачками по сегментам**, не по одному лиду. Выдавать компактной
   таблицей, принимать решения голосом, применять группой.
5. Ответы и UI — по-русски (см. `CLAUDE.md`).

---

## Фаза 0 — Бэкап и оживление

- [x] Дамп прод-базы: `turso db shell mitja-crm .dump > backups/2026-09-01-mitja-crm.sql`
      (создать `backups/`, добавить в `.gitignore`)
- [x] `npm install`, затем `npm run build` — убедиться, что проект собирается
- [x] Синк прод → `local.db`, чтобы локальная разработка шла на реальных данных
- [x] Гигиена git: `git config core.fileMode false`, добавить `._*` и `.DS_Store`
      в `.gitignore`, убрать шум из `git status` (права не коммитить)
- [x] Поднять `npm run dev` (порт 3002) и глазами проверить, что страницы живы

**Итог Фазы 0 (2026-09-01).** Бэкап `backups/2026-09-01-mitja-crm.sql` (61 КБ,
93/131/1/9/24/0 — сходится с фактами). `npm install` + `npm run build` — зелёные.
`local.db` пересобран из дампа прода (было 89 лидов от 2026-05-28, стало 93,
`max(updated_at)` = 2026-06-09); прежняя копия сохранена в
`backups/local.db.pre-sync-2026-09-01`. Git: `core.fileMode false` + `._*`,
`.DS_Store`, `/backups` в `.gitignore` → 313 записей в `git status` стали 2.
Все страницы отвечают 200 (`/` → 307 на `/today`).

**Отдельная находка — exFAT ломает Turbopack.** `next dev` падал:
`Failed to open database → Loading persistence directory failed → invalid digit
found in string`. Это не база — libsql открывает `local.db` нормально.
Диск `/Volumes/SSD 2` отформатирован в **exFAT**, macOS заводит AppleDouble-файл
`._*` для каждого файла с расширенными атрибутами, а turbo-persistence парсит
имена файлов в своём хранилище как числа и спотыкается о них.

`rm -rf .next` — лечение симптома: файлы возвращаются при следующей записи кэша,
поэтому первый запуск проходит, а второй падает. Что ещё не сработало: симлинк
всего `.next` на внутренний диск — Turbopack начинает писать туда чанки, и оттуда
уже не резолвится `node_modules` проекта (500 на `@tailwindcss/postcss`).

**Рабочее решение:** увести на внутренний диск только `.next/dev/cache` —
персистентный кэш Turbopack, — оставив чанки в проекте:

    rm -rf .next && mkdir -p .next/dev
    mkdir -p ~/Library/Caches/mitja-crm-turbocache
    ln -s ~/Library/Caches/mitja-crm-turbocache .next/dev/cache

Проверено: переживает перезапуск, все страницы 200, `._*` внутри кэша — ноль.
Симлинк придётся пересоздавать после `rm -rf .next`. Настоящее лечение — вернуть
репо на внутренний диск (APFS); решать Дмитрию.

## Фаза 0.5 — Выгрузка почты из Microsoft 365 (Graph API)

**Зачем.** `next_action_due` пуст, даты `outreach_events` кривые, а «сотни
клиентов» из открытого вопроса №1 — это адреса в «Отправленных». Почта = источник
правды по реальным касаниям: кому писали, когда, каким по счёту фоллоуапом,
ответили ли. Переборку базы (Фаза 1) имеет смысл делать уже с этими данными.

**Проверено 2026-09-01.** Локальный профиль Outlook на Mac
(`~/Library/Group Containers/UBF8T346G9.Office/…/Outlook.sqlite`, 5869 писем)
— это **старый рабочий аккаунт `dmitry.gubin@restate.ee`, янв 2020 – июнь 2022**.
Аутрича 2026 там нет, этот путь закрыт. Нужный ящик — рабочий Microsoft 365
(свой домен), в который Дмитрий залогинен в браузере. Решение: Graph API,
delegated-доступ, device code flow.

- [x] Скрипт `scripts/graph-mail.ts` + `npm run mail:sync` (device code, кэш
      refresh-токена в `.graph-token.json`, ретраи на 429/5xx, чекпоинт `--resume`)
- [x] `/data/mail` и `.graph-token.json` в `.gitignore` — почта в репозиторий не едет
- [x] Регистрация приложения в Entra, `MS_CLIENT_ID` + `MS_TENANT_ID` в `.env.local`
- [x] `npm run mail:sync` — 576 писем → `data/mail/messages.ndjson`
- [x] `npm run mail:sync -- --bodies` — 2.4 МБ тел ответов → `data/mail/bodies.ndjson`
- [ ] Разбор выгрузки: домен/адрес → лид, дата последнего исходящего,
      номер фоллоуапа, факт и дата ответа
- [ ] Сверка с базой: кто есть в почте, но не в CRM (ответ на открытый вопрос №1)
- [ ] Из этого — починка `outreach_events` и простановка `next_action_due`
      (переносится сюда из Фазы 1, т.к. чинить надо по фактам почты, а не на глаз)

**Итог выгрузки (2026-09-01).** Ящик `gubin@pforzwald.com` (не `restate.ee`).
576 писем: Sent 364, Inbox 199, Deleted 13; 381 исходящее, 195 входящих, 10 черновиков.
Период **2025-07-21 … 2026-08-31** — то есть аутрич шёл и после 9 июня, когда CRM
замолчала. Пики: окт 2025 (172), мар 2026 (105), ноя 2025 (86).

**Сверка с базой.** 121 уникальный адресат исходящих, 102 домена.
75 адресов есть в CRM, **46 — нет**. 10 адресов CRM в почте не встречаются
(писались не отсюда либо не писались вовсе). Отвечали 78 адресов.

**Три находки.**
1. «Сотни клиентов» = 93 лида + 46 адресов из почты ≈ 140. Из 46 минус шум
   (`gendin@pforzwald.com` — коллега, `gubi.dima@gmail.com` — сам Дмитрий,
   `lilpirita@yahoo.com`) остаётся ~43 реальных лида на заведение.
2. **Пайплайн продажи здания уже запущен** — 7–21 августа 2026 ушли письма
   в `realius.de`, `ave-immobilien.de`, `property-one.de`, `lc-immo.de`,
   `schwaebische-bauwerk.de`, `w-liegenschaften.de`, `etc-gruppe.de`,
   `gaiser-grundbesitz.de`, `mmg-grundbesitz.de`, `mh8.gmbh`, `grundschmiede.de`.
   Два ответа (realius 04.08, ave-immobilien 05.08) **висят без обработки 25 дней**.
   Фазу 3 не «ждём таблицу от Дмитрия», а собираем из почты.
3. **5 лидов ответили, но статус в CRM этого не знает:** #52 Rage AXE (6 ответов,
   посл. 09.05), #79 Helden, #43 Escape Rooms Pforzheim, #53 AXE ZONE,
   #26 Exit Box VR. Плюс #166 CinemaxX и #56 Doll Pilates — переписка шла
   в конце июня / начале июля, уже после последнего касания CRM.

**Границы.** Тела всех писем не тянем — только входящие в тредах, где было наше
исходящее (это и есть ответы). Метаданные + превью хватает для восстановления
хронологии касаний.

## Фаза 1 — Переборка существующей базы (голосом)

- [x] Пройти по сегментам, выдавая таблицами
- [x] По каждому: мёртвый / жив / когда следующее касание
- [x] Проставить `next_action_due` живым — **живых не оказалось, ставить некому**
- [x] Починить даты `outreach_events`
- [x] Развести живой пайплайн и архив — **весь старый пайплайн оказался архивом**

**Баг с датами — закрыт.** `parseDateToken` в `src/lib/import/parseStatus.ts`
для токена без года («27.10») ставил год импорта без проверки. Импорт шёл с
`--ref-date=2026-05-25`, поэтому «четвёртый follow up 27.10» превращался в
`2026-10-27` — пять месяцев в будущем — вместо `2025-10-27`. Правка: если дата
без года попадает в будущее относительно даты импорта, откатываем на год назад
(явно указанный год не трогаем). В проде сдвинуто 39 событий у 18 лидов,
событий с датой в будущем не осталось.

**Дубли событий.** Найдено 5 лишних строк у трёх лидов. Импорт ни при чём:
это ручные клики по кнопкам статуса 25.05 — у Ecclesia пять событий за четыре
секунды (12:17:43–47), у EFG два в одну секунду, у Cinema City два подряд.
Удалено 5 (4 + дубль Cinema City). У Boulderwelt пара оставлена: между
событиями 23 минуты, на случайный клик не похоже.
**Не починено:** кнопки быстрой смены статуса (`StatusQuickButtons.tsx`) никак
не защищены от повторного клика. Пока правится руками — стоит закрыть отдельно.

### Прогресс переборки (2026-09-01)

Договорённость по статусам: ответил и отказал → `replied_not_interested`,
молчит / не ответил → `dead`. Причина закрытия пишется в `notes` с датой.

| Сегмент | Итог |
|---|---|
| Скалодромы (13) | закрыт. 9 мёртвых + 4 отказа. «Никому не интересно: либо не ответили, либо отказались, либо пропали после „подумаем"» |
| Кино (12) | закрыт. 7 мёртвых + 5 отказов. Кто вёл переписку — отказ: не тот фокус, второй зал не потянут, рынок в Пфорцхайме перенасыщен. Семеро — переписка в LinkedIn без ответа, давняя. Cineplex по наводке заводили и удалили: прямой конкурент с кинотеатрами в том же городе |
| Церкви (7) | закрыт. 3 мёртвых + 4 отказа. Via Pforzheim — единственный интерес — отказались |
| Падель (7) | закрыт. 2 мёртвых + 5 отказов. **Причина структурная:** падель в кинозалах не строится по техническим параметрам здания. Сегмент как направление закрыт, не только эти лиды |
| Пилатес (7) | закрыт. 3 мёртвых + 4 отказа. Отказались все, включая тех, у кого был интерес |

| Comedy (6) | закрыт. 5 мёртвых + 1 отказ (Quatsch — сотрудничество на невыгодных условиях) |
| Лазертаг (6) | закрыт. 2 мёртвых + 4 отказа. Fun Sport («совместный бизнес») и Lasertag Weinstadt отвечали, но до сделки не дошло |
| Батуты (6) | закрыт. 3 мёртвых + 3 отказа. Sprungpark отвечал, дальше не пошло |
| Хвост (21) | закрыт. 17 мёртвых + 4 отказа. Отвечали XN Le BM («ведём переписку»), 7th Space («предлагают сотрудничество»), Tai Chi, Ahimsa Yoga |

## Итог Фазы 1 — база разобрана полностью

**93 лида: 52 `dead`, 41 `replied_not_interested`, открытых 0, `next_action_due`
не проставлен никому.**

Главный вывод: восстанавливать было нечего. Гипотеза из шапки плана была в том,
что инструмент не звал обратно и поэтому работа встала. Разбор показал другое —
**работа встала потому, что старый пайплайн был выработан до конца.** Ни один из
93 лидов не дожил до сделки; все, с кем шёл диалог, либо отказались, либо
перестали отвечать. Пустая страница «Сегодня» была не багом, а честным ответом.

Поэтому `next_action_due` остаётся пустым, и это правильное состояние базы,
а не недоделка. «Сегодня» оживёт на новых лидах, не на этих.

**Что база даёт для следующей кампании — причины отказов повторяются:**

1. **Технические параметры здания.** Падель в кинозалах не строится (сегмент
   закрыт целиком, а не по лидам). «Невозможно разместить в кинозале» — дважды.
2. **Размер площадей.** «200 м² слишком мало», «нужно 2500 м²», «нужно от
   2000 м²». Наши 100–250 м² отсекают крупных операторов на входе.
3. **Рынок Пфорцхайма.** Кинооператоры: перенасыщено, второй зал не потянут.
   Церкви: «есть своё здание», «нет такой нужды».
4. **Деньги и таймлайн.** «Нет финансирования», «только открылись», «помещение
   на реконструкции» — интерес есть, ресурса нет.

Пункты 1–2 — про здание, их надо учесть в оффере до следующей рассылки, иначе
следующая сотня лидов отвалится по тем же причинам.

**Потери, зафиксированные при разборе:** Kletterarena обещали связаться «на
следующей неделе» и не получили ответа 99 дней; Fun Sport предлагали совместный
бизнес — тоже 99 дней тишины; у Helden (id 79) в заметке значилась встреча Арины
16.04.2026, результат в CRM так и не попал. Всё это потеряно не из-за отказов,
а из-за того, что три месяца никто не возвращался.

**Что в этих трёх пачках стоит внимания:** Fun Sport (id 24, лазертаг) —
«единственный клиент, хотят делать совместный бизнес», висит 99 дней;
Quatsch Comedy Club (id 67) — предлагали сотрудничество, но невыгодные условия
(отказ по цене, а не по интересу); Sprungpark (id 20, батуты) — «есть интерес,
хотят обсудить», презентация отправлена.

## Фаза 1.5 — Появилось в репозитории по ходу сессии (не мной)

`scripts/graph-mail.ts` (311 строк) + `mail:sync` в `package.json` — выгрузка
почты из Microsoft 365 через Graph API, device-code-логин, метаданные писем в
`data/mail/messages.ndjson`, опционально тела входящих. Автор — не этот агент.
Скорее всего это и есть ответ на открытый вопрос №1 («сотни клиентов, а в базе
93»): реальная переписка из Outlook даст факты о том, кто и когда отвечал,
вместо реконструкции по заметкам. Требует `MS_CLIENT_ID` и `MS_TENANT_ID`
в `.env.local`. **Решить с Дмитрием, входит ли это в текущий план.**

## Фаза 2 — Шаблоны писем

- [ ] Занести с слов Дмитрия 2–3 шаблона (`initial` / `fup1` / `fup2`), RU + DE
- [ ] Проверить, что генерация письма и RU→DE перевод работают на реальном лиде
- [ ] Шаблон `initial` под продажу здания — **черновик уже есть**, лист «Поиск»
      в `Lumiera_Kaeuferliste_1.xlsx`, блок «ЧТО ПИСАТЬ В ПЕРВОМ СООБЩЕНИИ»
      (версия 21.08.2026): продаём здание, а не участок; параметры объекта;
      четыре арендатора; кино как 3 200 м² связной площади; слово «stabil» про
      доход не использовать; Baurecht одной фразой; закрытие — «ответьте Да»

## Фаза 3 — Новый пайплайн: продажа здания

- [x] Таблица получена: `Lumiera_Kaeuferliste_1.xlsx` (3 листа, 75 покупателей)
- [x] **Модель решена:** поле `deal_type` + `priority` на лиде, категории таблицы
      как обычные сегменты
- [x] Импорт 75 лидов + 19 событий из почты в прод
- [ ] Шаблоны под продажу (немецкий, тон под инвестора) — материал готов,
      см. Фазу 2

**Модель (решение 2026-09-01).** Победил вариант «поле, а не сегмент»:

- `leads.deal_type` (`rent` | `sale`, дефолт `rent`) — 93 старых лида получили
  `rent`, 75 новых `sale`. Индекс `idx_leads_deal_type`, фильтр в списке лидов.
- `leads.priority` (`high` | `medium` | `low`) — колонка «Приоритет» из таблицы.
  Внутри одной даты сортирует очередь в «Сегодня»: 65 новых лидов валятся на
  «написать первое» одним днём, и без приоритета верх списка был бы случайным.
- 15 новых сегментов по категориям таблицы (`investor_valueadd`,
  `fund_asset_manager`, `broker`, `self_storage`, `cinema_precedent`, …),
  `sort_order` с 300 — идут после арендных. Всего сегментов стало 39.
- Миграция колонок — `maybeAddLeadColumns` в `scripts/migrate.ts` (ALTER TABLE
  по `pragma table_info`, идемпотентно). `schema.sql` обновлён для чистых баз.

Категория «Уже отработаны» сегментом не стала: это стадия, а не тип покупателя.
Восемь её строк разложены по смыслу — REALIUS, ENDREV/ETC и Property-One в
`investor_family_office`, остальные пять в `investor_valueadd`.

**Импорт.** `scripts/import-investors.ts` — читает `.xlsx` напрямую (свой
мини-распаковщик zip на `node:zlib`, без новых зависимостей), дефолт — превью,
запись по `--apply`, повторный запуск пропускает уже заведённые (сверка по
`normalizeCompany` и email). Прогнан на копии `local.db`, затем на прод, затем
на `local.db`. Бэкап прода перед записью — `backups/2026-09-01b-pre-investors.sql`.

**Прод после импорта: 168 лидов (93 аренда + 75 продажа), 145 событий,
39 сегментов.** Дублей с арендным пайплайном ноль — пересечений по компаниям
и адресам между двумя списками нет.

**Статусы взяты из почты, а не из таблицы.** В файле колонки «Дата отправки» и
«Дата ответа» пустые, поэтому 19 событий восстановлены из
`data/mail/messages.ndjson` (Фаза 0.5):

| Лид | Событие | Статус |
|---|---|---|
| REALIUS | письмо 04.08, ответ 04.08, наш ответ 07.08 | `replied_not_interested` |
| AVE Immobilien | письмо 04.08, ответ 05.08, наш ответ 07.08 | `replied_not_interested` |
| LC Gruppe, Schwäbische BauWerk, W. Liegenschaften, ETC, Property-One | письмо 04.08 + фоллоуап 07.08 | `fup1_sent` |
| Gaiser Grundbesitz | письмо 07.08 (первый домен был неверный) | `contacted` |
| MH8, Grundschmiede | письмо 21.08 | `contacted` |
| остальные 65 | касаний нет | `new` |

Два расхождения таблицы с фактом, исправлены в пользу почты:
**Grundschmiede** — в таблице «1. Найти контакт», хотя письмо ушло 21.08 на
`kontakt@grundschmiede.de` (адрес тоже подставлен из почты, в таблице пусто);
**Gaiser** — в таблице «Follow-up отправлен», в почте одно письмо.

**«Сегодня» ожил.** Восемь лидов в «Просрочено»: пять ждут fup2 с 17.08,
Gaiser — fup1 с 14.08, MH8 и Grundschmiede — fup1 с 28.08. Это первое, что
увидит Дмитрий, и ровно то, чего не хватало базе после Фазы 1.

**Что осталось из таблицы вне CRM.** Лист «Поиск» — поисковые запросы, прецеденты
конверсий кинозданий и метод поиска покупателей через отраслевую прессу — в базу
не заводился, это методичка, а не лиды. Лист «Сводка» — воронка и легенда,
дублирует то, что CRM теперь считает сама.

---

## Открытые вопросы к Дмитрию

1. ~~**«Сотни клиентов», а в базе 93.** Где остальные?~~ → Гипотеза принята:
   остальные в «Отправленных» рабочего M365. Проверяется в Фазе 0.5.
2. Есть отдельная Turso-база `project-investors` — она пересекается с продажей
   здания или это другой проект?
3. Ежедневный ввод с телефона — решать после практики Фазы 1, не заранее.
