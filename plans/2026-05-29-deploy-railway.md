# План: Деплой на Railway + база Turso + защита паролем

**Статус:** `активный`

**Старт:** 2026-05-29

**Проблема:** сайт жил только на локальном `next dev` (порт 3002) — падал при закрытии
терминала/VS Code. Нужен постоянный хостинг 24/7 + облачная база (локальный `local.db`
не переживает редеплой).

**Решения (согласовано с Дмитрием):** хостинг — Railway; база — Turso (libsql);
сайт закрыт basic auth.

---

## Фаза 1 — Код
- [x] `src/middleware.ts` — basic auth (логин/пароль из `BASIC_AUTH_USER`/`BASIC_AUTH_PASS`)
- [ ] Проверить продакшн-сборку `next build`
- [ ] Закоммитить, создать GitHub repo, запушить

## Фаза 2 — База Turso
- [ ] Установить turso CLI, залогиниться
- [ ] Создать БД, получить URL + auth token
- [ ] Применить схему (`npm run migrate` против Turso URL)
- [ ] Перенести данные из `local.db`

## Фаза 3 — Railway
- [ ] Установить railway CLI, залогиниться
- [ ] Создать проект, привязать к репо
- [ ] Прописать env: `LIBSQL_URL`, `LIBSQL_AUTH_TOKEN`, `ANTHROPIC_API_KEY`, `BASIC_AUTH_USER`, `BASIC_AUTH_PASS`
- [ ] Задеплоить, получить домен, проверить
